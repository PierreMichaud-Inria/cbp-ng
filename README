Harcom heavily uses C++20 features, you need a recent compiler.
GCC (recommended): version 12 or later
Clang: version 21 or later

chmod u+x compile

test the library:
./compile test_harcom
./test_harcom

simulate the default TAGE predictor:
./compile cbp
./cbp

try another predictor:
compile cbp -DPREDICTOR="gshare<16,10>"
